/**
 * @description A constant is used here because the action are written
 * with string and are prone to errors and typo so that why it is declared
 * as a constant
 */

export const ADD_ARTICLE  = "ADD_ARTICLE";
